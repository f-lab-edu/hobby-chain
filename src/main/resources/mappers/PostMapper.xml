<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hobby.chain.post.domain.mapper.PostMapper">
    <resultMap id="postWithImages" type="com.hobby.chain.post.dto.PostDTO">
        <id property="postId" column="post_id"/>
        <result property="userIdx" column="user_idx"/>
        <result property="postContent" column="post_content" javaType="String"/>
        <collection property="images" ofType="imageList" resultMap="imageList"/>
    </resultMap>

    <resultMap id="imageList" type="com.hobby.chain.post.dto.ImageDTO">
        <result property="postId" column="post_id"/>
        <result property="imagePath" column="image_path"/>
        <result property="fileSize" column="file_size"/>
    </resultMap>

    <insert id="insertPost" parameterType="com.hobby.chain.post.dto.PostDTO">
        insert into post(user_idx, post_content, regdate)
        values (#{userIdx}, #{postContent}, NOW())
    </insert>

    <insert id="insertImages" parameterType="com.hobby.chain.post.dto.ImageDTO">
        insert into post_image(post_id, image_path, file_size)
        values (#{postId}, #{imagePath}, #{fileSize})
    </insert>

    <select id="isExistsImage" parameterType="long" resultType="boolean">
        SELECT EXISTS(
            SELECT post_id
            FROM post_image
            WHERE post_id = #{postId})
        as Result
    </select>

    <select id="getAllPost" resultMap="postWithImages">
        select p.post_content, p.regdate, i.image_path, i.file_size
        from post as p
        LEFT join post_image as i
        on p.post_id = i.post_id
    </select>

    <select id="getPostWithImage" resultMap="postWithImages">
        select p.post_content, p.regdate, i.image_path, i.file_size
        from post as p
        LEFT join post_image as i
        on p.post_id = i.post_id
        where p.post_id = #{post_id}
    </select>

    <select id="getPostWithoutImage" resultType="com.hobby.chain.post.dto.PostDTO">
        select post_content, regdate from post
        where post_id = #{post_id}
    </select>

    <select id="getAllPostWithoutImage" resultType="com.hobby.chain.post.dto.PostDTO">
        select * from post
    </select>

</mapper>